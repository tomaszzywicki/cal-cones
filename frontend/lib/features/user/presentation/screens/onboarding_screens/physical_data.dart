import 'package:flutter/material.dart';

class PhysicalData extends StatelessWidget {
  const PhysicalData({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Center(child: Text("Physical data")));
  }
}
