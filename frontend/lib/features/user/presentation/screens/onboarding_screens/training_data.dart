import 'package:flutter/material.dart';

class TrainingData extends StatelessWidget {
  const TrainingData({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Center(child: Text("Training data")));
  }
}
