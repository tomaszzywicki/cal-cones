import 'package:flutter/material.dart';

class GoalSetup extends StatelessWidget {
  const GoalSetup({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Center(child: Text("Goal setup")));
  }
}
